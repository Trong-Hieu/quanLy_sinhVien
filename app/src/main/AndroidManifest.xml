<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quanlysinhvien">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".admin_show_student_detail"></activity>
        <activity android:name=".admin_manage_student" />
        <activity android:name=".admin_add_activity" />
        <activity android:name=".admin_show_activity_detail" />
        <activity android:name=".UserInfor" />
        <activity android:name=".home_show_activity_detail" />
        <activity android:name=".user_activity_show_detail" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>